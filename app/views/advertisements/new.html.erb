<h1>New Advertisement</h1>

<div class="row">
    <div class="col-md-4">
        <p>Guidelines for advertisements</p>
        <ul>
            <li>Make sure to add an url link next to your bio</li>
            <li>Use this space to pitch us! not a full bio</li>
            <li>Every great work of art began as a whimsical pipe dream!</li>
        </ul>
    </div>
    <div class="col-md-8">
        <%= form_for @advertisement do |f| %>
            <div class="form-group">
                <%= f.label :title %>
                <%= f.text_field :title, class: 'form-control', placeholder: "Enter advertisement title" %>
            </div>
            <div class="form-group">
                <%= f.label :copy %>
                <%= f.text_area :copy, rows: 8, class: 'form-control', placeholder: "Enter advertisement body" %>
            </div>
            <div class="form-group">
                <%= f.label :price %>
                <%= f.text_field :price, class: 'form-control', placeholder: "Enter advertisement price" %>
            </div>
            <div class="form-group">
                <%= f.submit "Save", class: 'btn btn-success' %>
            </div>
        <% end %>
    </div>
</div>
